[project]
name = "uo-news-ner"
version = "0.1.0"
description = "NER research project for UO-News"
requires-python = ">=3.10"
dependencies = [
    "datasets>=2.14.0",
    "accelerate>=0.24.0",
    "seqeval>=1.2.2",
    "spacy>=3.7.0",
    "setuptools>=80.0.0",
    "wheel>=0.41.0",
    "scikit-learn>=1.3.0",
    "pandas>=2.1.0",
    "numpy>=1.25.0",
    "tqdm>=4.66.0",
    "jupyter>=1.0.0",
    "ipykernel>=6.27.0",
    "beautifulsoup4>=4.15.0",
    "requests>=2.32.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
    "ruff>=0.1.0",
]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
